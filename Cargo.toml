[workspace]
resolver = "2"
members = [
    "core",
    "sdk/ts/wasm",
    "sdk/native",
    "sdk/native/demo",
]

[workspace.dependencies]
ahash = "0.8"
getrandom = { version = "0.3", features = ["wasm_js"] }
