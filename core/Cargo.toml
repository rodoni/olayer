[package]
name = "olayer-core"
version = "0.1.0"
edition = "2021"

[dependencies]
quick-xml = "0.31.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "geodesy_benchmark"
harness = false
