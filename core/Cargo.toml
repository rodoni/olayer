[package]
name = "olayer-core"
version = "0.1.0"
edition = "2021"

[dependencies]
# Nenhuma dependência externa necessária para a geodésia.

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "geodesy_benchmark"
harness = false
